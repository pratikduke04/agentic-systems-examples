# Coding Examples — Study Order

This file records the recommended sequence for studying the coding examples.

The actual directory names are kept aligned with the instructor's repository
so that future updates can be synchronized easily.

## Earlier Exercises

1. Module 3 exercises

## LLM and LangChain

25. `ollama`
26. `langChain`
27. `langChain_worktree`
28. `langChain_tool`
29. `langChain_agent`
30. `langChain_memory`
31. `langChain_rag`
32. `langChain_integrated_agent`
33. `langChain_agent_evaluation`
34. `langChain_agent_debugging`
35. `langChain_hr_agent`

## Multi-Agent Systems

36. `multi_agent_crewai`
37. `multi_agent_http_automation`

## Other Examples

The following examples are also available but have not yet been assigned a study sequence:

- `api_call`
- `improving_rag`
- `rag_pipeline`
- `shopkart_rag`
- `tool_integration`
- `vector_search_lab`

## Personal Configuration

Machine-specific configuration such as API keys, local model names, and
credentials should not be committed to Git.

Use `.env` for local configuration wherever possible.